package com.practice;

public interface TDBank {
  // my application(TDBank name) all common method i put into the interface.
  // interface by default abstract and public

	void login();
	void transection();
	void accountsummary();
	void monthlystatement();
	void logout();
	
}
